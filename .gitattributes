# Auto normalize line endings for all text files
# Repository stores files with LF (Unix)
# Each platform checks out with native line endings (LF on Linux, CRLF on Windows)
* text=auto

# Shell scripts should always have LF
*.sh text eol=lf
